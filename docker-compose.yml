version: '3'

services:
  api:
    image: your_api_image
    ports:
      - '4001:4001'
    env_file:
      - .env.example

  web:
    image: your_web_image
    ports:
      - '5174:5174'
    env_file:
      - .env.example
